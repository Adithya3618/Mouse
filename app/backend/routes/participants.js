const express = require('express');

const router = express.Router();

// Placeholder for future participant-management endpoints
// (create/lookup participant records). Not implemented yet, and not
// mounted in server.js - the current app identifies participants only by
// the free-text "code" field saved with each score.

module.exports = router;
