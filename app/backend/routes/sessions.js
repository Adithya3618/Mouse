const express = require('express');

const router = express.Router();

// Placeholder for future experiment-session endpoints (create/fetch a
// research session's raw + processed data). Not implemented yet, and not
// mounted in server.js - the current app persists a session's results
// directly to Excel via routes/exports.js.

module.exports = router;
