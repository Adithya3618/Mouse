const express = require('express');

const router = express.Router();

// Placeholder for future experiment-definition endpoints (e.g. listing
// available experiments/conditions once more than one experiment exists).
// Not implemented yet, and not mounted in server.js.

module.exports = router;
