// Placeholder for a future database connection (e.g. SQLite/Postgres) as
// the platform grows beyond flat-file Excel export. Not implemented yet -
// the current app persists data directly to Excel via
// services/exportService.js.

module.exports = {};
